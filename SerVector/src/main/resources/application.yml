quarkus:
  tls:
    trust-all: true
  smallrye-openapi:
    path: /vector/openapi
  swagger-ui:
    always-include: true
    path: /vector/swagger
  container-image:
    group: iesd
    name: servector
    tag: 1.0

"%prod":
  hostname: ${HOSTNAME}
  quarkus:
    rest-client:
      service-lu:
        url: "http://lookup-service:9003"

"%dev":
  hostname: "127.0.0.1:9100"
  quarkus:
    rest-client:
      service-lu:
        url: "http://127.0.0.1:9003"
    http:
      host: 127.0.0.1
      port: 9100